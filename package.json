{
  "name": "assert-rejects",
  "version": "0.1.0",
  "license": "MIT",
  "repository": "LinusU/assert-rejects",
  "scripts": {
    "test": "standard && mocha"
  },
  "dependencies": {
    "is-regexp": "^1.0.0"
  },
  "devDependencies": {
    "mocha": "^3.1.0",
    "standard": "^8.2.0"
  }
}
